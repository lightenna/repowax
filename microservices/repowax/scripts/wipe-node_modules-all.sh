#!/usr/bin/env bash

# change into the script directory, if not there already
cwd="$(dirname $(readlink -f "$0"))"
cd $cwd

# find and list all the node_modules directories
find ../.. -name "node_modules" -type d -prune

echo "This script will DESTROY all the node_modules folders listed"
echo "Press Ctrl+C to abort."
read discarded_var

# find and run npm update on all package.json (minus exclusions)
find ../.. -name "node_modules" -type d -prune -exec rm -rf '{}' +